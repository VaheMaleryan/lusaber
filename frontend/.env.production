# Lusaber · Լուսաբեր — production env for the Vite build.
#
# This file is read by `vite build` (no quotes around the value).
# Update the URL after Render finishes the first backend deploy.

VITE_API_URL=https://lusaber-api.onrender.com
